## 初始化
   建议使用 `yarn`  
```shell
    # 初始化安装
    npm i -g lerna cross-env
    
    # 或者
    yarn add  -g lerna cross-env  -W
    
    # 安装项目所需要的包
    yarn bootstrap
```
## 启动
```shell
    # 编译并启动,开发启动单元测试
    yarn dev
    # 直接启动 不包含单元测试
    yarn storybook
```
